import { FilterApprovalPipe } from './filter-approval.pipe';

describe('FilterApprovalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterApprovalPipe();
    expect(pipe).toBeTruthy();
  });
});
